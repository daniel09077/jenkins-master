all:
  children:
    lxc:
      hosts:
        dev-server:
          ansible_host: 192.168.1.170
          ansible_user: ansible
          ansible_ssh_private_key_file: ~/.ssh/ansible_keys
        
        localhost:
          ansible_connection: local